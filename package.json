{
  "name": "react-gh-pages-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://loganarnett.github.io/react-gh-pages-test",
  "devDependencies": {
    "bluebird": "^3.4.6",
    "gh-pages": "^0.12.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "router-deploy": "npm run build && node config && npm run deploy"
  }
}
